# Web framework
fastapi==0.136.1

# Email validation (required by pydantic[email] / EmailStr)
email-validator==2.3.0
uvicorn[standard]==0.47.0

# Async ORM + PostgreSQL driver
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0

# Database migrations
alembic==1.18.4

# Auth: JWT + password hashing
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1 (dropped __about__)

# Async HTTP client (Open Food Facts API)
httpx==0.28.1

# File upload support for FastAPI
python-multipart==0.0.29

# S3-compatible photo storage (Supabase Storage / Cloudflare R2)
boto3==1.43.11

# Testing
hypothesis==6.152.9
pytest==9.0.3
pytest-asyncio==1.3.0
anyio[trio]==4.13.0
